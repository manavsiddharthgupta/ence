{
  "name": "ence",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "build": "dotenv -- turbo build",
    "dev": "dotenv -- turbo run dev",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "tsconfig": "*"
  },
  "dependencies": {
    "dotenv-cli": "^7.3.0"
  }
}
